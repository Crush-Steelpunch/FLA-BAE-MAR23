events {}
http{
    upstream backend {
        server {{ serverip1 }} weight=5;
        server {{ servierip2 }};
    }
    server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}